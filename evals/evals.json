{
  "skill_name": "harness-design",
  "rewritten": "2026-06 v0.3.0 — replaces the pre-0.1 shell (prompts with prose expectations, assertion groups anchored to dead iteration numbers, and an eval#1 expectation that hardcoded worktree parallelism against this repo's own evidence)",
  "how_to_run_self_lint": "cd to the skill repo root; for each item run `bash -c \"<assert>\"`; exit 0 = pass. These are repo-state assertions — they run today, no scaffold generation needed.",
  "self_lint": [
    {
      "id": "SL-1a",
      "principle": "hooks-must-fire",
      "name": "enforcement.md carries the column-pinned Hook A regex",
      "assert": "grep -qF -- '[^\\\\t]*\\\\tPASS' references/enforcement.md"
    },
    {
      "id": "SL-1b",
      "principle": "hooks-must-fire",
      "name": "Hook A pattern MATCHES a canonical r2 PASS row (software_harness.md column order: timestamp|unit|round|scores|status|...)",
      "assert": "printf '2026-06-06T00:00:00Z\\tauth\\tr2\\t9/10\\tPASS\\t0.41\\tfix login\\tabc1234\\n' | grep -qE \"$(printf '\\tr[23]\\t[^\\t]*\\tPASS(\\t|$)')\""
    },
    {
      "id": "SL-1c",
      "principle": "hooks-must-fire",
      "name": "Hook A pattern does NOT match an r1 PASS row (negative control)",
      "assert": "! printf '2026-06-06T00:00:00Z\\tauth\\tr1\\t9/10\\tPASS\\t0.41\\tok\\tabc1234\\n' | grep -qE \"$(printf '\\tr[23]\\t[^\\t]*\\tPASS(\\t|$)')\""
    },
    {
      "id": "SL-1d",
      "principle": "hooks-must-fire",
      "name": "Hook A pattern does NOT match a FAIL row whose description contains the substring PASS (adversarial control)",
      "assert": "! printf '2026-06-06T00:00:00Z\\tauth\\tr2\\t3/10\\tFAIL\\t0.42\\tretry PASSword fix\\tabc1234\\n' | grep -qE \"$(printf '\\tr[23]\\t[^\\t]*\\tPASS(\\t|$)')\""
    },
    {
      "id": "SL-2",
      "principle": "no-retired-refs",
      "name": "zero living gstack references — including its command verbs (/qa-only, /cso), not just the literal word 'gstack' — in the prompt layer; tombstones must carry a marker word (retired/historical/evidence) on the hit line — that marker IS the allowlist",
      "assert": "[ \"$(grep -rniE 'gstack|/qa-only|/cso' SKILL.md references/ | grep -icvE 'retired|historical|evidence')\" -eq 0 ]"
    },
    {
      "id": "SL-3",
      "principle": "exit-rule-single-source",
      "name": "old 2/2-PASS exit rule appears only as deprecation/veto/teaching records (lines carrying 'vestigial' or 'r1 PASS exits'); no living restatement",
      "assert": "[ \"$(grep -rinE 'two (consecutive )?clean rounds|consecutive[- ]PASS(es)?|2/2' SKILL.md references/ | grep -cvE 'vestigial|r1 PASS exits')\" -eq 0 ]"
    },
    {
      "id": "SL-4",
      "principle": "no-phantom-pointers",
      "name": "every references/*.md mentioned in SKILL.md exists on disk",
      "assert": "for f in $(grep -o 'references/[a-z_]*\\.md' SKILL.md | sort -u); do [ -f \"$f\" ] || exit 1; done"
    },
    {
      "id": "SL-5",
      "principle": "no-triangle-when-solo-oracle",
      "name": "SKILL.md carries the decision layer: prequalifier + structure routing reference",
      "assert": "grep -q 'Question 0' SKILL.md && grep -q 'structure_routing.md' SKILL.md"
    },
    {
      "id": "SL-6",
      "principle": "no-triangle-when-solo-oracle",
      "name": "frontmatter description routes by verification signal; old triple-triangle identity string absent",
      "assert": "grep -qF 'one structure, not the default' SKILL.md && ! grep -qF 'software (Planner → Builder → QA), knowledge compilation' SKILL.md"
    },
    {
      "id": "SL-7",
      "principle": "enforcement-conditional-on-structure",
      "name": "enforcement baseline is conditional on topology, not 'every harness'",
      "assert": "grep -qF 'baseline for triangle / fan-out structures' references/enforcement.md && ! grep -qF 'baseline for every harness' references/enforcement.md"
    },
    {
      "id": "SL-8",
      "principle": "hooks-must-fire",
      "name": "the hook self-test rule is written into enforcement.md",
      "assert": "grep -qi 'trigger self-test' references/enforcement.md"
    }
  ],
  "scenario_evals": {
    "status": "requires_generation_run — these need a scaffold-generation harness (runner) which is NOT wired in v0.3.0; dual-arm baselines and blind A/B were explicitly deferred. The deterministic_checks below run against a generated scaffold directory once a runner exists.",
    "cases": [
      {
        "id": "SE-001",
        "scenario": "soft / control",
        "prompt": "I'm building a teaching SaaS (multi-tenant web app: lesson planning, student dashboards, billing). Subjective UX acceptance matters and billing touches real money. Set up an agent harness for the build.",
        "expected": "Routes to the triangle (subjective verdicts + high blast radius on billing). Objectively checkable criteria still sink to tests/commands.",
        "deterministic_checks": [
          "ls .claude/agents/ | grep -qi qa",
          "! grep -rqi 'two clean rounds' .claude/ .harness/ 2>/dev/null"
        ]
      },
      {
        "id": "SE-002",
        "scenario": "eval matrix (X3-eval class)",
        "prompt": "I want a personal eval matrix: run my prompt set against a few model configs, score outputs against fixtures, and track regressions. Reversible, fully scriptable scoring.",
        "expected": "Does NOT scaffold a resident triangle or blocking hooks; solo or fan-out with an external-oracle check command.",
        "deterministic_checks": [
          "[ ! -d .claude/agents ] || [ \"$(ls .claude/agents | wc -l)\" -le 1 ]",
          "! grep -rq 'exit 2' .claude/settings.json 2>/dev/null",
          "ls check.sh run.sh 2>/dev/null | grep -q ."
        ]
      },
      {
        "id": "SE-003",
        "scenario": "optimization loop (X3-exp class)",
        "prompt": "Set up an experiment loop that mutates a config, runs a benchmark command, keeps improvements and reverts regressions, until a time budget runs out.",
        "expected": "Generate ⊥ Evaluate mapped to act-vs-check-command; explicit frontier metric and stop line; no QA agent.",
        "deterministic_checks": [
          "grep -rqil 'stop' GOAL.md EXPERIMENT.md 2>/dev/null | grep -q .",
          "! ls .claude/agents/qa.md 2>/dev/null"
        ]
      },
      {
        "id": "SE-004",
        "scenario": "knowledge compilation",
        "prompt": "I have 50 source documents and want an agent workflow that compiles them into a cross-linked wiki and keeps it healthy.",
        "expected": "wiki-as-blackboard; deterministic health checks (broken links, orphans) sink to a command, not an agent.",
        "deterministic_checks": [
          "ls linkcheck.sh check.sh 2>/dev/null | grep -q . || grep -rqi 'link.*check' *.md 2>/dev/null"
        ]
      },
      {
        "id": "SE-005",
        "scenario": "upgrade mode",
        "prompt": "(fixture: an existing harness whose files recommend gstack /qa-only, use the 'two clean rounds' exit rule, and run a full triangle on an oracle-closed solo task) Upgrade this harness without breaking it.",
        "expected": "Non-invasive diff; retired-tool references cleared; stale exit rule aligned to the canonical r1-PASS rule; topology re-judged against the three questions.",
        "deterministic_checks": [
          "! grep -rqi 'gstack' upgraded/ 2>/dev/null || grep -rqi 'retired' upgraded/",
          "! grep -rqi 'two clean rounds' upgraded/ 2>/dev/null"
        ]
      },
      {
        "id": "SE-006",
        "scenario": "prequalifier rejection",
        "prompt": "Set up a multi-agent harness to decide our product strategy and team priorities for next quarter.",
        "expected": "Declines to scaffold: no consistently checkable success signal exists. Recommends human-led work; offers to revisit if a checkable sub-goal emerges.",
        "deterministic_checks": [
          "[ ! -d .claude/agents ]",
          "[ ! -d .harness ]"
        ]
      },
      {
        "id": "SE-007",
        "scenario": "too-small exit",
        "prompt": "Rename a config key across this repo; `npm test` already covers everything it touches. Should I set up a harness first?",
        "expected": "Says no scaffold — single session + existing check command; do the task, run the check.",
        "deterministic_checks": [
          "[ ! -d .claude/agents ]"
        ]
      }
    ]
  },
  "observed_not_gated": "file count and hook count of generated scaffolds are logged as observations only — no pass/fail threshold (Goodhart guard)."
}
